The public product name and executable command surface are memory and memory-mcp. The npm package, repository, and domains intentionally remain @aictx/memory, github.com/aictx/memory, aictx.dev, docs.aictx.dev, and demo.aictx.dev. Canonical project storage uses .memory, the user registry uses MEMORY_HOME or ~/.memory, and legacy .aictx project storage migrates automatically when safe.