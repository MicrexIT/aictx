The public product command surface remains `memory` and `memory-mcp`, with package/repo/domain identity under Aictx. Public website, docs, README, SEO, and crawler-facing copy should lead with `Memory by Aictx` as the canonical product/entity and describe it as local, reviewable, auto-maintained project memory for AI coding agents. Keep package and CLI facts in supporting or canonical-surface copy instead of the hero. Avoid non-endorsement/disclaimer language in hero, footer, SEO, README, and docs identity copy; keep identity language calm and factual. Public SEO should still naturally cover high-intent adjacent terms: persistent memory for AI coding agents, project memory, agent memory, MCP memory server, Codex memory, Claude Code memory, Cursor memory, and AI coding assistants. Source-owned crawler/agent surfaces are part of the product surface: the main site serves `robots.txt`, `sitemap.xml`, and `llms.txt`; the docs site serves `robots.txt` pointing to the Starlight sitemap index and keeps generated `llms.txt` outputs. Origin robots files intentionally use `Content-Signal: search=yes, ai-input=yes, ai-train=yes` for AI crawler discoverability, but Cloudflare managed robots settings can still prepend blocking rules in deployment and must be disabled or adjusted in Cloudflare for the live domains to reflect the origin policy.