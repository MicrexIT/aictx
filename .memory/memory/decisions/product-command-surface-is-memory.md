The public product command surface remains `memory` and `memory-mcp`, with package/repo/domain identity under Aictx. Public SEO and crawler-facing copy should optimize around `Memory by Aictx` as the canonical product/entity, keep identity language calm and factual, and expose source-owned crawler/agent surfaces: the main site serves `robots.txt`, `sitemap.xml`, and `llms.txt`; the docs site serves `robots.txt` pointing to the Starlight sitemap index and keeps generated `llms.txt` outputs.