The tag-triggered release workflow publishes npm first, then computes the SHA-256 from the published npm tarball, renders Formula/memory.rb with scripts/render-homebrew-formula.mjs, and pushes the formula to aictx/homebrew-tap. The workflow requires HOMEBREW_TAP_TOKEN in the npm GitHub environment with write access to the tap and checks out the tap before npm publish so missing access fails early.