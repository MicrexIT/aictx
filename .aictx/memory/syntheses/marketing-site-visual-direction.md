The public landing page should use the warm light painted hero treatment with a single centered hero card, not the README visual-asset collage. The primary hero headline is: "Stop re-explaining your repo to AI agents." Keep the hero headline to two visible lines maximum: desktop should break as "Stop re-explaining your" / "repo to AI agents.", while mobile should break as "Stop re-explaining" / "your repo to AI agents." Supporting copy should stay short: "Aictx saves durable project knowledge as reviewable repo memory." It should stay on one line on desktop and split on mobile as "Aictx saves durable project knowledge" / "as reviewable repo memory." Keep a single primary hero CTA, Install Aictx, with the install command as a quiet supporting detail. The install command and primary CTA should be visually related but not inside one stretched shared pill: use a compact command pill beside a separate CTA on desktop, and stack the two cleanly on mobile without cropping the command. Do not put a Browse demo CTA in the hero; keep the live demo available in the lower demo/product section. Keep the before/after slider section titled "Project memory for Codex." between the context section and How it works. Do not use the README preview card, hero pills, or README workflow screenshot on the landing page. The How it works section should use native site cards/flow. The demo/product section should use /assets/viewer-demo.png unless the viewer screenshot is intentionally refreshed. The site chrome labels the article/use-case listing as Use cases and points to /use-cases/, while individual articles may still live under /blog/.