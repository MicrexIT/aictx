Agents should right-size memory: use source records for provenance, syntheses for compact area-level understanding, and atomic memories for precise reusable claims. Saves create active memory immediately after Aictx validates and writes the patch; there is no normal human review gate, draft queue, or final diff-review step. Agents should report whether memory changed, and humans can inspect accepted memory asynchronously through the viewer, `aictx diff`, or Git tools. Agents should update existing memory when appropriate, mark stale or supersede wrong memory, delete memory that should not persist, and save nothing when work produced no durable future value.