# Bootstrap links product features from the project

Bootstrap now links deterministic product-feature concepts from the owning project with `implements` relations. This keeps setup-generated feature nodes reachable by graph browsing and one-hop retrieval while avoiding generic `related_to` overuse.
