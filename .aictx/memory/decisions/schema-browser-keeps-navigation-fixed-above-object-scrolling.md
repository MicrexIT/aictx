The local viewer schema browser intentionally keeps the canonical object filters and compact trust/status summary above the embedded relation overview, then constrains browsing scroll to the memory workspace below the graph. The large page hero is visually hidden on this screen, and warnings or starter-memory notices live inside the scrollable workspace so they cannot block object row interaction on mobile.