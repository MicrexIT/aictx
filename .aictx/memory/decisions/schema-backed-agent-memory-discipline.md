# Schema-backed agent memory discipline

Aictx storage schema v2 adds object-level facets and evidence, indexes facet/evidence material, renders facet-aware context sections, and provides `aictx suggest --after-task` plus `aictx upgrade` so agents can make regular save/no-save reviews without turning AGENTS.md into detailed taxonomy documentation.