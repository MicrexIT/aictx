# Agent guidance is CLI-first and MCP-compatible

Aictx guidance now presents `aictx load`, `aictx save --stdin`, and `aictx diff` as the default routine agent workflow. MCP remains implemented and documented as a supported equivalent path only when the client has already launched and connected to `aictx-mcp`.