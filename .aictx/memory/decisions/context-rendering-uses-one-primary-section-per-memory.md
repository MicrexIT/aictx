Context packs assign each selected active memory object to one primary rendered section by default: capped `Agent guidance`, architecture snapshots in architecture mode, facet/type sections, then `Must know` as the fallback. `Relevant files` renders only file-like applicability, file evidence, and file paths found in bodies; non-file `applies_to` values render under `Relevant areas`.