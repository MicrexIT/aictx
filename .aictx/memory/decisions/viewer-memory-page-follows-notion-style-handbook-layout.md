The local viewer memory page should read like a document-style coding handbook, not a maintenance cockpit. Use a fixed warm document sidebar with project stats, search, and page navigation; a centered `Coding Handbook` document hero; a generated-handbook callout; pill-style filters; and expandable row cards that reveal memory details inline. Memory rows should toggle open and closed in place without navigating to a separate detail route or auto-scrolling the viewport. Keep accents neutral and document-like: avoid green success/current-state accents in favor of black ink and warm gray states.