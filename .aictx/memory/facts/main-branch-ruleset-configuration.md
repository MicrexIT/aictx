GitHub repository ruleset 16127697 (`push main`) protects the default branch. As of 2026-05-08 it is active, has no bypass actors, includes creation/deletion/non-fast-forward protections, requires pull requests with 0 approving reviews for collaborator self-merge, and requires strict status checks from GitHub Actions app id 15368: `Node 22`, `Node 24`, and `Analyze JavaScript and TypeScript`. The `update` restriction is intentionally omitted so PR merges can update main through normal GitHub merge flow while direct pushes remain blocked by the PR/status rules.